# HouseCall Backend

Go services for the HouseCall platform — Core API Service, AI Agent Runtime, and
Integration Workers (see [`../docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) §2).

**Status:** not yet implemented. The Phase 1 MVP build is being planned as an
OpenSpec change proposal under `../openspec/changes/`. No code lives here yet —
this directory is scaffolded so the repo layout is ready for backend work.
